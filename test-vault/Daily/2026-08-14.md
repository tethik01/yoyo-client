# 2026-08-14

Built the MCP layer for [[Yoyo]] today. Four bugs worth remembering:

- StringIO has no fileno, so it cannot be a subprocess stderr on Windows
- mcp 2.0 renamed FastMCP to MCPServer
- the SDK moved camelCase to snake_case, which silently emptied tool arguments
- an empty YOYO_VAULT_PATH made the working directory the vault

Restore drill passed 11/11 on the USB drive. BitLocker still off.
